import { HtmlSeguroPipe } from './html-seguro.pipe';

describe('HtmlSeguroPipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlSeguroPipe();
    expect(pipe).toBeTruthy();
  });
});
